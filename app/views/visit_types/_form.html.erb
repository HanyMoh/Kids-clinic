<%= form_with(model: visit_type, local: true) do |form| %>
  <% if visit_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(visit_type.errors.count, "error") %> prohibited this visit_type from being saved:</h2>

      <ul>
      <% visit_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= form.label :name, "نوع الكشف" %>
    <%= form.text_field :name, id: :visit_type_name, class: "form-control" %>
  </div>

  <div class="actions">
    <%= form.submit "حفظ", class: "btn btn-primary" %>
  </div>
<% end %>

<div class="col-md-6">
<div class="page-header">
  <h1>
    <small>عرض أحدث 5</small>
     زيارات</h1>
</div>
<table class="table table-striped table-bordered">
  <thead>
    <tr>
      <th class="text-center">م</th>
      <th class="text-center">التاريخ</th>
      <th class="text-center">اسم الحالة</th>
      <th class="text-center">نوع ىالكشف</th>
      <th class="text-center">ألدور</th>
    </tr>
  </thead>

  <tbody>
    <% @last_shank.each do |visit| %>
      <tr>
        <td><%= link_to visit.code, visit  %></td>
        <td><%= visit.visit_date %></td>
        <td><%= visit.patient&.name %></td>
        <td><%= visit.visit_type&.name %></td>
        <td><%= visit.turn_num %></td>
      </tr>
    <% end %>
  </tbody>
</table>
</div>
<div class="col-md-6">
  <div class="page-header">
    <h1>
      <small>عرض شغل اليوم من </small>
       الزيارات</h1>
  </div>
  <table class="table table-striped table-bordered">
    <thead>
      <tr>
        <th class="text-center">م</th>
        <th class="text-center">التاريخ</th>
        <th class="text-center">اسم الحالة</th>
        <th class="text-center">نوع ىالكشف</th>
        <th class="text-center">ألدور</th>
      </tr>
    </thead>

    <tbody>
      <% @current_visits.each do |visit| %>
        <tr>
          <td><%= link_to visit.code, visit  %></td>
          <td><%= visit.visit_date %></td>
          <td><%= visit.patient&.name %></td>
          <td><%= visit.visit_type&.name %></td>
          <td><%= visit.turn_num %></td>
        </tr>
      <% end %>
    </tbody>
  </table>
  </div>
</div>
